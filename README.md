Add a .env file to the project root. 
Inside put
GEMINI_API_KEY="your api key here." does not have to be gemini. make sure models in generate content are changed to the model you are using
PROJECT_ROOT="file_path/sandbox" filepath to the directory that the agent can freely make changes in.
